{
  "name": "@bitty/root",
  "private": true,
  "scripts": {
    "test": "pnpm --filter ./packages run test",
    "build": "pnpm run build -r"
  },
  "devDependencies": {
    "pnpm": "^10.12.4"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "onFail": "error",
      "version": ">=22.14.0"
    },
    "packageManager": {
      "name": "pnpm",
      "onFail": "error",
      "version": ">=10.12.4"
    }
  }
}
