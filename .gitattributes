# File attributes for Git repository.
# References at https://git-scm.com/docs/gitattributes.

# Handle files as text and ensure Unix line endings.
* text=auto eol=lf

# Ignore differences on PNPM's lockfile.
# Since version 5.11.0, PNPM automatically resolve its merge conflicts.
pnpm-lock.yaml
